---
import SliderItem from '../atoms/SliderItem.astro';
import { LOGOS } from '../../constants/logos';
---

<div class='slider'>
	<div class='list-slider'>
		{
			LOGOS.map(({ name, position, icon }) => (
				<SliderItem
					src={icon}
					label={name}
					position={position}
				/>
			))
		}
	</div>
</div>
